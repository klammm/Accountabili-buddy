import Dimensions from 'Dimensions';
import Image from 'react-native-scalable-image';


const ImageScaleable = ({ sourceUrl, width }) => {
  return (
    <ImageScaleable
      width={width}
      source={source}
    />
  );
}
